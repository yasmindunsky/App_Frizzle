<!--\\u000A-->
<!--’-->

<practice xmlns:tools="http://schemas.android.com/tools"
tools:keep="@xml/practice_1">
    <slide>
        <info_text>The speakOut command instructs the computer to read text aloud.</info_text>
        <task_text/>
        <reminder_text />
        <code show="true" mutable="false" runnable="true" waitForCTA="false">speakOut("Hello");</code>
        <design show="false" runnable="false" withOnClickSet="false"></design>
        <call_to_action>Got it</call_to_action>
    </slide>


    <slide>
        <info_text/>
        <task_text>Change the command so it will speak out your name.</task_text>
        <reminder_text />
        <code show="true" mutable="true" runnable="true" waitForCTA="true">speakOut("Hello");</code>
        <design show="false" runnable="false" withOnClickSet="false"></design>
        <call_to_action>Check</call_to_action>
    </slide>

    <slide>
        <info_text>Rewrite the command to speak out:
'this is so cool'. </info_text>
        <task_text/>
        <reminder_text>Reminder: speakOut("Hello"); </reminder_text>
        <code show="true" mutable="true" runnable="true" waitForCTA="false">\n</code>
        <design show="false" runnable="false"  withOnClickSet="false"></design>
        <call_to_action>Check</call_to_action>
        <solution>speakOut("this is so cool");</solution>
    </slide>

    <slide>
        <info_text/>
        <task_text>Delete the semicolon at the end of the command and try to run the code.</task_text>
        <reminder_text/>
        <code show ="true" mutable="true" runnable="true" waitForCTA="true">speakOut("Hello");</code>
        <design show="false" runnable="false" withOnClickSet="false"></design>
        <call_to_action>Got it</call_to_action>
    </slide>

    <slide>
        <info_text/>
        <task_text>Type a space character between the ’speak’ and ’out’ in the command name and run the code.</task_text>
        <reminder_text/>
        <code show="true" mutable="true" runnable="true" waitForCTA="true"> speakOut("Hello");</code>
        <design show="false" runnable="false" withOnClickSet="false"></design>
        <call_to_action>Got it</call_to_action>
    </slide>


</practice>
